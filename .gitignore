# Dependencies
node_modules/

# Build output (regenerable: npm run build)
dist/
.vite/
www/

# iOS native project (we don't ship iOS)
ios/

# Android — keep the project itself, ignore only build artifacts and
# machine-specific config so the entrega cumple el enunciado UOC.
android/.gradle/
android/.idea/
android/build/
android/app/build/
android/captures/
android/local.properties
android/app/release/
android/app/debug/

# Logs and environment
*.log
.env
.env.local

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp

# Capacitor / Claude Design exports (saved HTML mocks)
*.html
!index.html
*_files/
